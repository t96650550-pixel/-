{
  "name": "chat-app-server",
  "version": "1.0.0",
  "description": "Web chat giống messenger có admin, socket.io, React frontend",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "helmet": "^6.0.1",
    "jsonwebtoken": "^9.0.0",
    "socket.io": "^4.7.2",
    "sqlite3": "^5.1.6"
  },
  "engines": {
    "node": ">=18.x"
  }
}
